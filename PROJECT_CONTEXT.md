# PROJECT_CONTEXT.md

## Project Name
Shared Cockpit Project

## One-Sentence Goal
Create a reliable shared working memory so ChatGPT can plan and Codex can
implement without losing context between sessions.

## Current Product Goal
Replace this section with the real project goal.

Example:

> Build an AI comic or drama production tool that manages character locks,
> prompts, negative prompts, scene prompts, and storyboard prompts.

## Target Users
- The project owner.
- ChatGPT as planning partner.
- Codex as implementation partner.

## Collaboration Model
- ChatGPT handles strategy, task breakdown, prompt design, product direction,
  acceptance criteria, and review checklists.
- Codex handles code implementation, local files, tests, debugging, and
  technical documentation updates.
- The user makes final decisions when product direction or taste is ambiguous.

## Product Principles
- Keep decisions explicit.
- Prefer small, testable tasks.
- Write enough context that either ChatGPT or Codex can continue without
  guessing.
- Update shared memory after each meaningful change.

## Technical Direction
Replace this section with the actual stack.

Example:

- Phase 1: local HTML, CSS, and JavaScript.
- Storage: localStorage first.
- Later: JSON import/export, then optional backend.

## UI Direction
Replace this section with confirmed visual references and style rules.

Example:

- Clean white and light gray interface.
- Large search entry points.
- Compact cards for repeated items.
- Blue text buttons for secondary actions.
- Avoid decorative layouts that slow down task work.

## Non-Goals
- Do not build unrelated features just because they are interesting.
- Do not change confirmed project direction without logging the decision.
- Do not rely on memory alone for important requirements.

